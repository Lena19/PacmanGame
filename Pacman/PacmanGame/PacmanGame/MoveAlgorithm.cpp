#include "MoveAlgorithm.h"

Coordinate MoveAlgorithm::Move(const Coordinate& currentCoordinate, const Coordinate& targetCoordinate, const std::vector<std::vector<CellOfMap>> map){
	//??????
}

const bool CheckCoordinate(const Coordinate& currentCoordinate, const std::vector<std::vector<CellOfMap>> map){
	//????
}
Coordinate Normalize(Coordinate& currentCoordinate, const std::vector<std::vector<CellOfMap>> map){
	//????
}